__pycache__/
*.py[cod]
*.db
*.db-shm
*.db-wal
*.xlsx
.env
venv/
.venv/

# Large installer - excluded from git
software/QT Insite/

# PyInstaller build output (the exe is a build artefact; it is published to the
# inventory server's package store, not committed to git)
build/
dist/
*.spec

# Cowork device-mount artefacts
.fuse_hidden*
