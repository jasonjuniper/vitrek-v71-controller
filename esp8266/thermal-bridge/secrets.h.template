// Copy this file to secrets.h and fill in your values.
// DO NOT commit secrets.h — it is listed in .gitignore.

#define WIFI_SSID      "your-wifi-ssid"
#define WIFI_PASSWORD  "your-wifi-password"

// Static IP for the bridge (must match rig_config.json esp8266.host).
// Set your router to assign this IP via DHCP reservation, or configure
// a static IP directly on the device.
#define BRIDGE_IP      "192.168.1.60"
#define BRIDGE_GATEWAY "192.168.1.1"
#define BRIDGE_SUBNET  "255.255.255.0"
